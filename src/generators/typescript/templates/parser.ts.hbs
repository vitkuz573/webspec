// @generated by webspec — DO NOT EDIT

import * as cheerio from 'cheerio';
import { {{entity_imports}} } from './models';

{{#each pages}}
export function parse{{this.pascal}}(html: string): {{this.return_type}} {
  const $ = cheerio.load(html);
{{#if this.list_selector}}
  const items: {{this.entity}}[] = [];
  $('{{escape this.list_selector}}').each((_: number, el: cheerio.Element) => {
    const $el = $(el);
    const item: {{this.entity}} = {
{{#each this.fields}}
      {{this.camel}}: {{{this.extraction}}},
{{/each}}
    };
    items.push(item);
  });
  return items;
{{else}}
  const item: {{this.entity}} = {
{{#each this.fields}}
    {{this.camel}}: {{{this.extraction}}},
{{/each}}
  };
  return item;
{{/if}}
}

{{/each}}
